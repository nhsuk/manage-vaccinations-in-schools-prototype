{% extends "_layouts/form.njk" %}

{% set title = __("consent.alternative." + session.programmePreset + ".title") %}

{% block form %}
  {{ radios({
    fieldset: {
      legend: {
        html: appHeading({
          classes: "nhsuk-fieldset__legend--l",
          title: title
        })
      }
    },
    hint: {
      text: __("consent.alternative." + session.programmePreset + ".hint")
    },
    items: [{
      text: __("consent.alternative." + session.programmePreset + ".yes.label"),
      value: true
    }, {
      text: __("consent.alternative." + session.programmePreset + ".no.label"),
      value: false
    }],
    decorate: "consent.alternative"
  }) }}
{% endblock %}
