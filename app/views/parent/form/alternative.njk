{% extends "_layouts/form.njk" %}

{% set type = "Flu" if session.programmePreset == "SeasonalFlu" else "MMR" %}
{% set title = __("consent.alternative." + type + ".title") %}

{% block form %}
  {{ radios({
    fieldset: {
      legend: {
        html: appHeading({
          classes: "nhsuk-fieldset__legend--l",
          title: title
        })
      }
    },
    hint: {
      text: __("consent.alternative." + type + ".hint")
    },
    items: [{
      text: __("consent.alternative." + type + ".yes.label"),
      value: true
    }, {
      text: __("consent.alternative." + type + ".no.label"),
      value: false
    }],
    decorate: "consent.alternative"
  }) }}
{% endblock %}
