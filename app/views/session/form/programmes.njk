{% extends "_layouts/form.njk" %}

{% set title = __("session.edit.programmes.title") %}

{% block form %}
  {{ checkboxes({
    fieldset: {
      legend: {
        classes: "nhsuk-fieldset__legend--l",
        html: heading({
          caption: session.location.name,
          title: title
        })
      }
    },
    items: programmeItems,
    decorate: "session.programmes"
  }) }}
{% endblock %}
