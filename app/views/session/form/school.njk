{% extends "_layouts/form.njk" %}

{% set title = __("session.school.title") %}

{% block form %}
  {{ appHeading({
    title: title
  }) }}

  {{ select({
    label: { text: __("session.school_urn.title") },
    items: schoolItems(schools, session.school_urn),
    decorate: "session.school_urn",
    attributes: {
      "data-module": "app-autocomplete"
    }
  }) }}
{% endblock %}
