{% extends "_layouts/form.njk" %}

{% set title = __("session.edit.school.title") %}

{% block form %}
  {{ heading({
    title: title
  }) }}

  {{ xGovukAutocomplete({
    id: "school-urn",
    name: "[session][school_urn]",
    value: session.school_urn,
    allowEmpty: false,
    label: { text: __("session.edit.school.label") },
    items: schoolUrnItems
  }) | replace("govuk-", "nhsuk-") }}
{% endblock %}
