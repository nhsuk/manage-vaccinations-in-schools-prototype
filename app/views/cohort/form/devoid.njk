{% extends "_layouts/default.njk" %}

{% set title = __("cohort.devoid.title") %}
{% set paths = {
  back: campaign.uri + "/cohorts/new"
} %}

{% block content %}
  <div class="nhsuk-u-width-three-quarters">
    {{ heading({
      caption: campaign.type,
      title: title
    }) }}

    {{ __("cohort.devoid.description") | nhsukMarkdown }}

    {{ link(campaign.uri + "/cohorts/new", __("cohort.new.label")) | nhsukMarkdown }}
  </div>
{% endblock %}
