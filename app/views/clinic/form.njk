{% extends "_layouts/form.njk" %}

{% set title = __("clinic." + type + ".title") %}
{% set confirmButtonText = __("clinic." + type + ".confirm") %}

{% block form %}
  {% call fieldset({
    legend: {
      html: appHeading({
        classes: "nhsuk-fieldset__legend--l",
        title: title
      })
    }
  }) %}
    {{ input({
      label: { text: __("clinic.name.label") },
      decorate: "clinic.name"
    }) }}

    {{ input({
      label: { text: __("address.addressLine1.label") },
      autocomplete: "address-line1",
      decorate: "clinic.address.addressLine1"
    }) }}

    {{ input({
      label: { text: __("address.addressLine2.label") + " (optional)" },
      autocomplete: "address-line2",
      decorate: "clinic.address.addressLine2"
    }) }}

    {{ input({
      classes: "nhsuk-u-width-two-thirds",
      label: { text: __("address.addressLevel1.label") },
      autocomplete: "addressLevel1",
      decorate: "clinic.address.addressLevel1"
    }) }}

    {{ input({
      classes: "nhsuk-input--width-10",
      label: { text: __("address.postalCode.label") },
      autocomplete: "postal-code",
      decorate: "clinic.address.postalCode"
    }) }}
  {% endcall %}
{% endblock %}
