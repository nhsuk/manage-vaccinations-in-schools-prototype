{%- from "_macros/icon.njk" import icon -%}
{% macro status(params) -%}
<p class="app-status{{ " app-status--" + params.colour if params.colour }}">
  {{- icon(params.icon) if params.icon }}{{ params.text -}}
</p>
{%- endmacro %}
