{% macro event(item, users) -%}
  {% if item.note -%}
  <blockquote>
    {{ item.note | nhsukMarkdown }}
  </blockquote>
  {%- endif %}
  <p class="nhsuk-body-s nhsuk-u-margin-0 nhsuk-u-secondary-text-color">
    {{ item.date | date({
      dateStyle: 'long',
      timeStyle: 'short',
      hourCycle: 'h12'
    }) }}
    {% if item.user_uid %}&middot;
      {{ link(users[item.user_uid].uri, users[item.user_uid].fullName) | safe }}
    {% endif %}
  </p>
{%- endmacro %}
