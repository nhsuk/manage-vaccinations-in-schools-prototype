{% extends "_layouts/form.njk" %}

{% set title = __("vaccination.sequence.title") %}

{% block form %}
  {{ radios({
    fieldset: {
      legend: {
        html: heading({
          classes: "nhsuk-fieldset__legend--l",
          caption: record.fullName,
          title: title
        })
      }
    },
    items: enumItems(VaccinationSequence),
    decorate: "vaccination.sequence"
  }) }}
{% endblock %}
