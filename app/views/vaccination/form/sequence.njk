{% extends "_layouts/form.njk" %}

{% set title = __("vaccination.sequence.title", vaccination.programme.name) %}

{% block form %}
  {{ radios({
    fieldset: {
      legend: {
        html: appHeading({
          classes: "nhsuk-fieldset__legend--l",
          caption: vaccination.patient.fullName,
          title: title
        })
      }
    },
    items: sequenceItems,
    decorate: "vaccination.sequence"
  }) }}
{% endblock %}
