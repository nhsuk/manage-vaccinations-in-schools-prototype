{% extends "_layouts/form.njk" %}

{% set title = __("vaccination.created.title") %}

{% block form %}
  {{ heading({
    caption: vaccination.patient.fullName,
    title: title
  }) }}

  {{ dateInput({
    fieldset: {
      legend: {
        classes: "nhsuk-fieldset__legend--m",
        text: __("vaccination.created_date.label")
      }
    },
    hint: { text: __("vaccination.created_date.hint") },
    decorate: "vaccination.created_"
  }) }}

  {{ dateInput({
    fieldset: {
      legend: {
        classes: "nhsuk-fieldset__legend--m",
        text: __("vaccination.created_time.label")
      }
    },
    hint: { text: __("vaccination.created_time.hint") },
    items: [{
      classes: "nhsuk-input--width-2",
      label: "Hour",
      id: "vaccination-created_-hour",
      name: "[vaccination][created_][hour]",
      value: vaccination.created_.hour
    }, {
      classes: "nhsuk-input--width-2",
      label: "Minute",
      id: "vaccination-created_-minute",
      name: "[vaccination][created_][minute]",
      value: vaccination.created_.minute
    }]
  }) }}
{% endblock %}
