{% extends "_layouts/form.njk" %}

{% set title = __("vaccination.createdBy.title") %}

{% block form %}
  {{ radios({
    fieldset: {
      legend: {
        html: heading({
          classes: "nhsuk-fieldset__legend--l",
          caption: vaccination.patient.fullName,
          title: title
        })
      }
    },
    items: userItems,
    decorate: "vaccination.createdBy_uid"
  }) }}
{% endblock %}
