{% extends "_layouts/form.njk" %}

{% set title = __("vaccination.decline.title", { session: session }) %}

{% block form %}
  {{ radios({
    fieldset: {
      legend: {
        html: heading({
          classes: "nhsuk-fieldset__legend--l",
          caption: vaccination.patient.fullName,
          title: title
        })
      }
    },
    items: declineItems,
    decorate: "vaccination.outcome"
  }) }}
{% endblock %}
