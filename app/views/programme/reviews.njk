{% extends "_layouts/default.njk" %}

{% set title = programme.name + " – " + __("programme.reviews.title") %}
{% set view = "reviews" %}

{% block beforeContent %}
  {{ breadcrumb({
    items: [{
      text: __("programme.list.title"),
      href: "/programmes"
    }],
    text: programme.name,
    href: programme.uri
  }) }}
{% endblock %}

{% block content %}
  {{ super() }}

  {{ heading({
    title: programme.name,
    summary: programme.year
  }) }}

  {% include "programme/_secondary-navigation.njk" %}

  <h2 class="nhsuk-heading-m nhsuk-u-visually-hidden">
    {{ __("programme.reviews.title") }}
  </h2>

  {% if reviews.length %}
    {% set reviewRows = [] %}
    {% for vaccination in reviews %}
      {% set reviewRows = reviewRows | push([
        {
          header: __("record.fullName.label"),
          html: vaccination.record.fullName
        },
        {
          header: __("vaccination.review.issue.label"),
          html: __("vaccination.review.issue.text")
        },
        {
          header: __("vaccination.review.actions.label"),
          html: actionList({
            items: [{
              text: "Review",
              href: vaccination.uri + "/review"
            }]
          })
        }
      ]) %}
    {% endfor %}

    {{ actionTable({
      id: "reviews",
      sort: "name",
      heading: __n("programme.reviews.count", reviews.length),
      panel: true,
      responsive: true,
      head: [
        {
          text: __("record.label"),
          attributes: { "no-sort": "no-sort" }
        },
        {
          text: __("vaccination.review.issue.label"),
          attributes: { "no-sort": "no-sort" }
        },
        {
          text: __("vaccination.review.actions.label"),
          attributes: { "no-sort": "no-sort" }
        }
      ],
      rows: reviewRows
    }) }}
  {% else %}
    {{ __n("programme.reviews.count", reviews.length) | nhsukMarkdown }}
  {% endif %}
{% endblock %}
