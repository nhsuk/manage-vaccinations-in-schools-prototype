{% extends "_layouts/form.njk" %}

{% set title = __("record.school.title") %}

{% block form %}
  {{ xGovukAutocomplete({
    id: "school-urn",
    name: "[record][school_urn]",
    value: consent.child.school_urn,
    allowEmpty: false,
    label: {
      classes: "nhsuk-label--l",
      html: heading({
        caption: patient.fullName,
        title: title
      })
    },
    items: urnItems
  }) | replace("govuk-", "nhsuk-") }}
{% endblock %}
