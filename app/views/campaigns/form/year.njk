{% extends "_layouts/form.njk" %}

{% set title = __("campaign.year.title") %}

{% block form %}
  {{ radios({
    fieldset: {
      legend: {
        classes: "nhsuk-fieldset__legend--l",
        html: heading({
          title: title
        })
      }
    },
    items: enumItems(CampaignYear),
    decorate: "campaign.year"
  }) }}
{% endblock %}
